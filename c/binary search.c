#include <stdio.h>
void main()
{
    int n,mid;
    printf("Enter a number to search : ")
    scanf("%d",&n)
    arr[5]={1,2,3,4,5}
    mid=[sizeof(arr)/sizeof(arr[0])]/2
    for(int i=0;i<5;i++)
    {
        if(n==arr[mid])
        {
            printf("found at index %d",arr[mid])
        }
        else
        {
            if n<arr[mid]
        }
    }
}